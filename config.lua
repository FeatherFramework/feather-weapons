Config = {
    -- NOTE: not currently read anywhere in this repo (grep-confirmed) --
    -- declared but unused, presumably reserved for a future
    -- weapon-restriction feature.
    blacklistedWeapons = {
        {
            weaponName = "WEAPON_PISTOL_M1899"
        }
    }
}